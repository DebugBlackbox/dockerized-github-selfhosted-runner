# ── Required ──────────────────────────────────────────────────────────────────

# GitHub organisation or username that owns the runner
GITHUB_OWNER=my-org

# Repository name inside the org (leave empty for an org-level runner)
GITHUB_REPO=dbwrap

# Personal Access Token with:
#   • repo scope (repo-level runner)  OR
#   • admin:org  scope (org-level runner)
# Tip: use a fine-grained PAT with "Self-hosted runners" → read & write permission
GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Optional overrides ────────────────────────────────────────────────────────

# Comma-separated labels applied to every runner (no spaces)
RUNNER_LABELS=self-hosted,linux,arm64

# Runner group (requires GitHub Teams / Enterprise for custom groups)
RUNNER_GROUP=Default

# Working directory inside the container where job checkouts live
RUNNER_WORKDIR=/tmp/_work
